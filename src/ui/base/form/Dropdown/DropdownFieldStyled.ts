import styled from 'styled-components'
import { Label as BaseLabel, LabelText as BaseText } from '../TextField/TextFieldStyled'

export const Label = styled(BaseLabel)``

export const LabelText = styled(BaseText)``
